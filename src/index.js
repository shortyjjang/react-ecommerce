import React from 'react';
import ReactDOM from 'react-dom';
import YesUsApp from './YesUsApp';

ReactDOM.render(
  <React.StrictMode>
    <YesUsApp />
  </React.StrictMode>,
  document.getElementById('root'),
);
